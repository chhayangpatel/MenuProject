---
import type { Restaurant } from '../../../schemas/restaurant.schema';
import { resolveLogo } from '../../../lib/assets';
interface Props { restaurant: Restaurant; }
const { restaurant } = Astro.props;
const { contact } = restaurant;
const logoUrl = resolveLogo(restaurant);
---
<header class="bs-nav-bar">
  <div class="bs-nav-inner">
    <a href="/" class="bs-back-link" aria-label="Back" style="display:inline-flex;align-items:center;justify-content:center;width:36px;height:36px;border-radius:0;color:rgba(255,255,255,0.6);transition:all 0.2s;background:rgba(255,23,68,0.1);"><svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="#FF1744" stroke-width="2.5"><line x1="19" y1="12" x2="5" y2="12"/><polyline points="12 19 5 12 12 5"/></svg></a>
    <img src={logoUrl} alt={restaurant.name} class="bs-nav-logo" />
    <span class="bs-nav-name">{restaurant.name}</span>
    <nav class="bs-nav-actions" style="display:flex;gap:0.5rem;" aria-label="Contact">
      {contact?.phone && <a href={"tel:"+contact.phone} class="bs-nav-action" style="display:inline-flex;align-items:center;justify-content:center;width:36px;height:36px;border-radius:0;color:rgba(255,255,255,0.6);transition:all 0.2s;background:rgba(255,23,68,0.1);" title="Call"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M22 16.92v3a2 2 0 0 1-2.18 2"/></svg></a>}
      {contact?.googleMapsUrl && <a href={contact.googleMapsUrl} target="_blank" rel="noopener" class="bs-nav-action" style="display:inline-flex;align-items:center;justify-content:center;width:36px;height:36px;border-radius:0;color:rgba(255,255,255,0.6);transition:all 0.2s;background:rgba(255,23,68,0.1);" title="Directions"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"/><circle cx="12" cy="10" r="3"/></svg></a>}
    </nav>
  </div>
</header>
<style>
  .bs-back-link:hover,.bs-nav-action:hover{color:#FF1744;background:#FF1744;transform:scale(1.1);}
  .bs-back-link:hover{transform:translateX(-3px);}
</style>